/** @deprecated Use PhotoAttachmentPicker — kept for backward compatibility */
import PhotoAttachmentPicker from "./PhotoAttachmentPicker";

export default function MemoryImageAttachments(props) {
  return <PhotoAttachmentPicker {...props} />;
}
